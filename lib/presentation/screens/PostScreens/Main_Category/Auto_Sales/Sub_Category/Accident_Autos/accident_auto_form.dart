import 'package:flutter/material.dart';

class AccidentAutoForm extends StatelessWidget {
  const AccidentAutoForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Accident Autos Form")),
      body: Text("Accident Autos Form"),
    );
  }
}