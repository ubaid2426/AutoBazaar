import 'package:flutter/material.dart';

class ScrapsAutosForm extends StatelessWidget {
  const ScrapsAutosForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Scraps Autos Form")),
      body: Text("Scraps Autos Form"),
    );
  }
}