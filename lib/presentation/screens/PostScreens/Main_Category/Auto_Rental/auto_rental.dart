import 'package:flutter/material.dart';

class AutoRental extends StatelessWidget {
  const AutoRental({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Auto Rental"),),
      body: Center(child: Text("Auto Rental")),
    );
  }
}