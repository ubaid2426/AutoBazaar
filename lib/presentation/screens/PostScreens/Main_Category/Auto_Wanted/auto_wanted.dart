import 'package:flutter/material.dart';

class AutoWanted extends StatelessWidget {
  const AutoWanted({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Auto Wanted"),),
      body: Text("Auto Wanted"),
    );
  }
}